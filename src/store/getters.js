//工具接口为了方便构建全局state自定义方法

/**
 * 官网页面总数据
 */
export const pageArr    = state => state.pageArr;
/**
 * 
 * 当前编辑项
 */
export const currentObj    = state => state.currentObj;

/**
 * 访问过的路由
 */

export const visitedViews    = state => state.visitedViews;
//mid
export const midObj    = state => state.midObj;

export const groupArr    = state => state.groupArr;
